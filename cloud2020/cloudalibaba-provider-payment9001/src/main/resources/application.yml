server:
  port: 9001

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        # 单机版
        # server-addr: 127.0.0.1:8848
        # 集群版
        server-addr: 127.0.0.1:1111

# 暴露监控端口
management:
  endpoints:
    web:
      exposure:
        include: "*"